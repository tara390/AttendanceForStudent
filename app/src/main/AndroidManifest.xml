<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tara.attendanceforstudent">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".Activities.Teacher.ChangePasswordActivity">

        </activity>
        <activity android:name=".Activities.Teacher.GetOtpActivity"></activity>
        <activity android:name=".Activities.Teacher.ForgetPasswordActivity"></activity>
        <activity android:name=".Activities.Subject.SubjectActivity"></activity>
        <activity android:name=".Activities.Subject.ExistingSubjectActivity"></activity>
        <activity android:name=".Activities.Subject.AddNewSubjectsActivity"></activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Activities.Student.ExisitingStudentActivity" />
        <activity android:name=".Activities.Student.AddNewStudentActivity" />
        <activity android:name=".Activities.Student.AddStudentActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.default_searchable"
                android:value="com.tara.attendanceforstudent" />
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ObjectDetectionActivity">
            <meta-data
                android:name="com.google.firebase.ml.vision.DEPENDENCIES"
                android:value="face" />
        </activity>
        <activity android:name=".Activities.Teacher.VerificationOtpscreenActivity" />
        <activity
            android:name=".Activities.DashboardActivity"
            android:label="@string/app_name" />
        <activity android:name=".Activities.Teacher.TeacherRegistrationActivity" />
        <activity android:name=".Activities.Teacher.LoginActivity">

        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:configChanges="keyboard|keyboardHidden|orientation">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>